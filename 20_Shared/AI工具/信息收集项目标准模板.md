# 信息收集项目标准模板

## 标准识别区

- 输入ID：20260608-shared-info-collection-template
- 输入时间：2026-06-08 00:00
- 来源类型：文档
- 来源名称：通用信息收集系统标准架构与执行流程
- 作者 / 机构：Codex
- 原始日期：2026-06-08
- 原始链接：暂无
- 项目归属：X
- 内容类型：资料
- 目标输出：报告 / 课程 / 周报 / 暂无
- 处理指令：整理
- 分类动作：X
- 优先级：高
- 状态：已入库
- 标签：信息收集, 项目模板, 标准模板, 可复用

## 一句话摘要

这是一个可复制到任何新信息收集系统的标准模板，用于快速定义目标、来源、数据模型、工作流、评分规则、输出渠道、安全规范和运行观测。

## 1. 项目定义

```text
项目名称：
项目代号：
所属项目：P1 / P2 / P3 / P4 / P5 / X
目标用户：
核心目标：
输出物：日报 / 周报 / 选题库 / 报告 / 脚本 / 预警 / API / 其他
运行频率：手动 / 每日 / 每周 / 实时 / 事件触发
```

## 2. 领域特异性配置

`[特异性配置]` 这里必须按领域填写，不允许空着。

```text
高价值内容定义：
低价值内容定义：
必须人工确认的内容：
需要优先关注的实体：
需要排除的来源或主题：
输出语言：
目标保存位置：
```

## 3. 信息源注册表

```text
source_id：
source_type：rss / github / web / api / social / database / local_file / manual
source_name：
url_or_endpoint：
enabled：true / false
project：P1 / P2 / P3 / P4 / P5 / X / 待判断
default_action：P / X / S / A / B / C / D
fetch_limit：
time_window：
requires_auth：true / false
api_key_env：
metadata_fields：
failure_policy：skip / retry / stop
```

## 4. 统一内容模型

```json
{
  "id": "",
  "source_type": "",
  "source_name": "",
  "title": "",
  "url": "",
  "content": "",
  "author": "",
  "published_at": "",
  "fetched_at": "",
  "language": "",
  "project": "",
  "content_type": "",
  "metadata": {},
  "score": null,
  "score_reason": "",
  "summary": "",
  "tags": [],
  "dedupe_key": "",
  "canonical_id": "",
  "status": "raw"
}
```

## 5. 评分规则

```text
评分范围：0-10
默认阈值：
高价值阈值：
是否启用 AI 评分：
是否启用规则评分：
是否启用人工复核：
```

评分维度：

- 领域相关性。
- 新鲜度。
- 价值度。
- 可输出性。
- 关联性。
- 热度或影响范围。
- 来源可信度。

## 6. 去重规则

```text
URL 去重：启用 / 禁用
标题相似去重：启用 / 禁用
主题级去重：启用 / 禁用
跨来源合并：启用 / 禁用
保留策略：最高分 / 最新 / 最完整 / 人工选择
```

## 7. 深度补全规则

`[成本控制]` 只对达到阈值的内容做深度补全。

```text
触发阈值：
补全内容：背景 / 实体 / 时间线 / 争议 / 社区讨论 / 参考链接
搜索来源：
最大补全条数：
人工核验要求：
```

## 8. 输出模板

```text
输出格式：Markdown / JSON / HTML / API / Webhook / Email
输出目录：
标题格式：
条目结构：
是否生成目录：
是否双语：
是否保留原始链接：
是否保留评分理由：
```

## 9. 分发渠道

```text
本地 Obsidian：启用 / 禁用
GitHub Pages：启用 / 禁用
邮件：启用 / 禁用
飞书/钉钉：启用 / 禁用
Slack/Discord：启用 / 禁用
Webhook：启用 / 禁用
MCP/API：启用 / 禁用
```

## 10. 安全与密钥

`[安全红线]` 禁止把真实密钥、cookie、token、Webhook URL 写入 Git 或 Obsidian。

```text
密钥存放方式：系统环境变量 / .env / Secret Manager
.env 是否加入 .gitignore：
config 是否只保存 env var 名：
泄露后轮换流程：
最小权限说明：
```

## 11. 运行观测

```text
是否记录 run_id：
是否保存 raw_items：
是否保存 scored_items：
是否保存 filtered_items：
是否保存 enriched_items：
是否记录 token/cost：
是否记录错误来源：
是否生成运行报告：
```

## 12. 测试清单

- 单个来源失败时，主流程是否继续。
- AI 评分失败时，是否保留原始内容。
- 输出渠道失败时，本地文件是否仍保存。
- 重复内容是否能合并。
- 高价值内容是否能进入输出。
- 低价值内容是否能被过滤。
- 密钥是否不会进入 Git。
- 是否能从中间阶段重跑。

## 13. 项目启动最小输入

```text
CMD:入库 | PROJECT:P4 | ACTION:P | OUTPUT:报告 | TARGET:10_Projects/04_AI前沿与Codex工具/04_可复用工作流 | PRIORITY:高

项目名称：
目标用户：
要收集什么：
信息源：
输出物：
运行频率：
评分标准：
分发渠道：
安全限制：
```

## 推荐去向

- 保存路径：`20_Shared/AI工具`
- 关联笔记：[[通用信息收集系统标准架构与执行流程]]
- 下一步动作：为每个新信息收集系统复制此模板并填写领域特异性配置。
